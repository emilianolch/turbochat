<h3>Rooms</h3>
<p>Enter one of the existing chat rooms or create a new one to start chatting.</p>
<div id="rooms" class="mb-3">
  <div class="list-group">
    <% @rooms.each do |room| %>
      <%= link_to room.name, room, class: 'list-group-item list-group-item-action' %>
    <% end %>
  </div>
</div>

<%= link_to "New room", new_room_path, class: 'btn btn-primary' %>
